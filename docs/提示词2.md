请在 app.js 中为所有搜索输入框（paramSearch、commandSearch、channelSearch）实现以下优化：

1. 【实时过滤不触发重渲染】
   - paramSearch 输入时：不调用 renderView()，直接遍历所有 tr[data-param-row]，根据搜索词匹配 textContent 决定 display:none 或 display:''
   - commandSearch 输入时：不调用 renderView()，直接遍历所有 article[data-command-card]
   - channelSearch 已有 filterRows() 实现，保持不变

2. 【防抖后同步 state 并完整渲染】
   每次输入后启动 300ms 防抖计时器，超时后：
   - state.tableSearch = input.value（同步 state）
   - renderView()（完整渲染，保证分页计数、状态栏等正确）
   - 恢复焦点

3. 【中文输入法兼容】
   compositionstart 时暂停实时过滤，compositionend 时触发一次完整过滤

请实现一个通用的 createLiveFilter(inputId, rowSelector, stateKey, renderDelay) 函数封装上述逻辑，然后在 bindViewActions() 中分别为三个搜索框调用它。