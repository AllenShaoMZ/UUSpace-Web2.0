# UUSpace Web 2.0 — 功能需求对比文档

> 基准版本：桌面软件 `D:\UUSpace1.0.0\SateliteController`（C# WinForms + WPF SciChart）
> 目标版本：Web 端 `D:\UUSpace1.0.0\UUSpace-Web2.0`（Node.js + 原生 HTML/CSS/JS）
> 文档日期：2026-05-18

---

## 目录

1. [遥测表格](#1-遥测表格)
2. [曲线绘制](#2-曲线绘制)
3. [状态显示](#3-状态显示)
4. [源码显示](#4-源码显示)
5. [指令界面](#5-指令界面)
6. [快捷键](#6-快捷键)
7. [优先级汇总](#7-优先级汇总)

---

## 1 遥测表格

### 1.1 桌面软件现有功能（TeleTable.cs）

| 功能 | 说明 |
|------|------|
| 自定义 TableView | 非系统 DataGridView，完全自绘，支持高 DPI |
| 列配置 | 序号、代号、名称、单位、公式、当前值、十六进制、状态、二进制、路序；每列可独立显示/隐藏、调整宽度 |
| 行拖拽排序 | 拖动行到目标位置重排（UUSpace.TableRowIndex 格式） |
| 列头拖拽排序 | 拖动列头重排列顺序（UUSpace.TableColIndex 格式） |
| 拖入遥测通道 | 从通道树拖入遥测名称自动添加行（UUSpace.TelemetryNames 格式） |
| 双击行 | 打开 SelectChFormEx 通道选择对话框 |
| Ctrl+A | 全选所有行 |
| 右键菜单 | 选择通道 / 选择显示列 / 删除选中行 / 设置小数位数 / 表格样式 |
| 小数位数设置 | 每行独立设置，-1 = 自动（8 位有效数字），0–12 位固定 |
| 表格样式 | 行高（16–120 px）、自适应列宽、单元格字体/大小/粗斜体、数值字体、数字格式（本地化/不变量） |
| 数值显示规则 | 绝对值 > 1e8 或 < 1e-4 时自动切换科学计数法 |
| 后台缓存优化 | 控件隐藏时缓存最新帧，下次显示时一次性应用，避免无效重绘 |
| 状态着色 | 正常/告警/关注三种状态对应不同行背景色 |

### 1.2 Web 端现有功能（app.js renderTelemetryTable）

| 功能 | 说明 |
|------|------|
| HTML `<table>` 渲染 | 标准 DOM 表格，CSS 样式 |
| Sheet 标签页 | 0–7 共 8 个 Sheet 切换（对应协议帧） |
| 搜索框 | 按代号/名称实时过滤 |
| 状态过滤 | 全部 / 告警 / 收藏 三个快速筛选按钮 |
| 自定义视图 | 可保存/切换多个命名视图（收藏行集合） |
| 曲线勾选 | 每行复选框，勾选后在曲线视图中显示该通道 |
| 原地 DOM 更新 | 收到新帧时仅更新值单元格，带 flash 动画 |
| 状态着色 | 告警行高亮 |

### 1.3 差距与需求

| 需求项 | 优先级 | 说明 |
|--------|--------|------|
| 列显示/隐藏配置 | 高 | 用户可选择显示哪些列（十六进制、二进制、路序等） |
| 列宽调整 | 高 | 拖动列头边缘调整宽度，持久化到 localStorage |
| 小数位数设置 | 中 | 右键菜单 → 设置小数位数（自动 / 固定 0–12 位） |
| 数值科学计数法 | 中 | 绝对值 > 1e8 或 < 1e-4 时自动切换 |
| 右键上下文菜单 | 中 | 删除行、设置小数位数、列配置入口 |
| 全选（Ctrl+A） | 低 | 见快捷键章节 |

---

## 2 曲线绘制

### 2.1 桌面软件现有功能

#### SciChart 版（SciChartHostedCurvePanel.cs）

| 功能 | 说明 |
|------|------|
| 渲染引擎 | WPF SciChart（商业库），DirectX 硬件加速 |
| 多通道 | 每面板最多 N 条曲线，每条独立颜色/线宽 |
| 双 Y 轴 | 左轴（index 0）/ 右轴（index 1），按通道 AxisIndex 自动显隐 |
| 悬停引导线 | 鼠标移动时显示垂直虚线 + 各系列当前值标签 + 底部时间标签 |
| 选择缩放 | Ctrl + 左键拖拽框选区域 → 弹出独立历史窗口（主曲线继续运行） |
| 历史弹窗 | 独立 Form，含 RealtimeCurveHost，可调 X 轴范围 |
| 还原视图 | Ctrl+Shift+R / 右键菜单"还原" → 回到最新实时视图 |
| X 轴范围 | 点击 X 轴 → WaveTimeAxisDialog，可设 5 s–604800 s（7 天） |
| Y 轴范围 | 点击 Y 轴 → WaveYAxisDialog，手动设上下限 |
| 标题编辑 | 点击标题区域 → 内联 TextBox 编辑 |
| 头部信息 | 卫星名称 + 协议时间标签 |
| 帧队列 | 最大 4000 帧，保留尾部 1200 帧；66 ms 前台刷新定时器 |
| 导出图片 | Ctrl+Shift+E → SaveFileDialog → PNG/JPG/BMP |
| 拖入通道 | 从通道树拖入遥测名称添加曲线 |
| 双击 | 打开通道选择对话框 |
| 运行/暂停 | F5 运行 / F6 暂停 |
| 清空数据 | Ctrl+Delete |
| 属性对话框 | Ctrl+Shift+P → 配置线宽、颜色、可见窗口等 |

### 2.2 Web 端现有功能（app.js renderCurve / drawCurveCanvas）

| 功能 | 说明 |
|------|------|
| 渲染引擎 | HTML5 Canvas 2D，requestAnimationFrame 循环 |
| 多通道 | 每页最多 6 通道，渐变填充 |
| 布局 | 1/2/3 列自适应，每格独立 Canvas |
| 数据缓冲 | 每通道 180 点环形缓冲 |
| 实时更新 | WebSocket 收帧后更新缓冲，下次 rAF 重绘 |
| 通道选择 | 遥测表格中勾选复选框 |

### 2.3 差距与需求

| 需求项 | 优先级 | 说明 |
|--------|--------|------|
| 悬停引导线 | 高 | mousemove 事件 → 垂直虚线 + 各通道值标签 + 时间标签 |
| 运行/暂停控制 | 高 | 工具栏按钮 + 快捷键（见快捷键章节） |
| 缩放与平移 | 高 | 鼠标滚轮缩放 X 轴；拖拽平移（暂停时） |
| 导出图片 | 中 | Canvas.toDataURL() → 下载 PNG |
| 标题编辑 | 中 | 点击标题区域内联编辑 |
| 历史弹窗 | 低 | 选区缩放后弹出独立历史视图（主曲线继续运行） |

---

## 3 状态显示

### 3.1 桌面软件现有功能（TeleFlowControl.cs）

| 功能 | 说明 |
|------|------|
| 卡片流布局 | 默认 190×100 px 卡片，DPI 自适应缩放 |
| 卡片内容 | 参数名称、代号、当前值、状态指示 |
| 拖拽排序 | 卡片间拖拽重排位置 |
| 拖入通道 | 从通道树拖入遥测名称添加卡片 |
| 双击 | 打开 SelectChForm 通道选择对话框 |
| 垂直滚动 | 卡片超出区域时可滚动 |
| 状态着色 | 正常/告警/关注三种卡片背景色 |

### 3.2 Web 端现有功能（app.js renderStatus）

| 功能 | 说明 |
|------|------|
| 统计磁贴 | 顶部 4 个大数字磁贴（总帧数、告警数、端口数、在线时长） |
| 端口矩阵 | UDP 端口状态网格，显示每端口帧率/状态 |
| 摘要条目 | 最多 6 个遥测参数摘要，含迷你进度条 |
| 事件流 | 实时事件日志列表（时间戳 + 描述） |
| 告警列表 | 当前告警参数列表 |

### 3.3 差距与需求

web端暂时不改动


---

## 4 源码显示

### 4.1 桌面软件现有功能（BinaryTextBook.cs）

| 功能 | 说明 |
|------|------|
| 专用视图 | 独立的"源码显示"标签页，不与其他视图混合 |
| 环形缓冲 | 最多 65536 帧历史 |
| Hex 网格 | 每字节显示为 2 位大写十六进制，固定网格布局 |
| 行索引标签 | 左侧显示行号（字节偏移） |
| 列号标签 | 顶部显示列编号（0–N） |
| 垂直滚动 | 滚动条浏览历史帧 |
| 可配置网格尺寸 | 默认 20×20 px，可调整 |

### 4.2 Web 端现有功能（app.js renderHexBytes）

| 功能 | 说明 |
|------|------|
| 内联 Hex 显示 | 嵌入在"连接"视图的事件流中，非独立视图 |
| 颜色分段 | 低值（蓝）/ 中值（绿）/ 高值（红）/ 淡色（灰）四色 |
| 包头高亮 | 协议包头字节特殊着色 |
| 每帧截断 | 最多显示 512 字节（server 端截断） |

### 4.3 差距与需求
web端暂时不改动



---

## 5 指令界面

### 5.1 桌面软件现有功能

> 注：桌面指令界面主要源文件（RemoteSetControl.cs）未在本次分析范围内完整读取，以下基于 realtime.js 中 mapCommandRow 及 app.js renderCommandCenter 的对应关系推断，并结合 cmdchain.txt 格式。

| 功能 | 说明 |
|------|------|
| 指令分类 | 星上指令（K 前缀）/ 动力学指令（D/DLX 前缀）/ 参数上注（P 前缀）/ 其他 |
| 指令链 | cmdchain.txt 定义多指令顺序执行链，含权重/延时 |
| 指令类型 | 10 种类型（间接/内部/单地址注数/立即软件注数/延时系列/连续地址注数等） |
| 目标配置 | 每条指令独立配置目标 IP + UDP 端口 |
| 可参数化模板 | DataSrc=-1 的指令支持运行时填入参数 |
| 固定源码 | DataSrc=1 的指令直接发送预设 HEX 包 |
| 节点标识 | 每条指令标注所属执行单元/软件节点 |

### 5.2 Web 端现有功能（app.js renderCommandCenter）

| 功能 | 说明 |
|------|------|
| 卡片网格 | 指令以卡片形式展示，含名称/代号/类型/节点 |
| 分类过滤 | 顶部标签按类别筛选（全部/星上指令/动力学指令/参数上注/其他） |
| 搜索 | 按代号/名称实时搜索 |
| 两步确认发送 | 点击卡片 → 详情面板 → 确认发送，防误触 |
| HEX 包显示 | 详情面板显示原始 HEX 报文 |
| 从 Excel 导入 | 通过 /api/commands 读取指令 XLS 文件 |
| POST 发送 | 调用 /api/command/send，服务端通过 UDP 发出 |
| 发送状态反馈 | 发送后显示成功/失败状态 |

### 5.3 差距与需求

| 需求项 | 优先级 | 说明 |
|--------|--------|------|
| 指令链执行 | 高 | 支持 cmdchain.txt 定义的多指令顺序链，含延时间隔配置 |
| 可参数化模板 | 高 | DataSrc=-1 的指令在发送前弹出参数填写表单 |
| 指令执行日志 | 高 | 记录每次发送的时间戳、指令代号、目标、结果 |
| 发送进度显示 | 中 | 指令链执行时显示当前步骤/总步骤进度条 |
| 指令链编辑 | 中 | 界面内编辑/保存 cmdchain.txt（增删改顺序） |
| 批量发送 | 低 | 多选指令后批量发送（含间隔配置） |
| 指令收藏 | 低 | 标记常用指令，快速访问 |

---

## 6 快捷键

### 6.1 桌面软件快捷键

#### 曲线视图（SciChart 版）

| 快捷键 | 功能 |
|--------|------|
| 双击 | 打开通道选择对话框 |
| Ctrl + 左键拖拽 | 框选区域 → 弹出放大缩小窗口 |
| Ctrl + 右键 | 还原缩放 + 恢复运行 |

#### 遥测表格视图

| 快捷键 | 功能 |
|--------|------|
| Ctrl+A | 全选所有行 |
| 双击行 | 打开通道选择对话框 |

### 6.2 Web 端现有快捷键

**当前 Web 端无任何键盘快捷键实现。**

### 6.3 差距与需求

| 快捷键 | 视图 | 优先级 | 说明 |
|--------|------|--------|------|
| Ctrl + 左键拖拽 | 框选区域 → 弹出放大缩小窗口 |
| Ctrl + 右键 | 还原缩放 + 恢复运行 |


---


---

*文档由 Kiro 自动生成，基于对桌面软件源码（C#）与 Web 端源码（JS）的静态分析。如有功能遗漏或描述偏差，请直接修改本文档。*
