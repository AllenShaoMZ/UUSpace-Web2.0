请优化 app.js 中的 renderConnection() 和 renderProtocol() 函数，以及 styles.css 中对应的样式：

【连接管理页优化】
1. UDP 接收验证区域的 .source-block 显示 HEX 数据，当前用白色等宽字体难以阅读。改为每个字节一个 span，字节值 00-3F 显示为深灰，40-7F 蓝色，80-FF 橙色，超出包头范围的字节淡化显示
2. port-map 端口状态格（port-pill）加入实时包速率显示（包/秒），有数据时格子高亮，长时间无数据变为警告色
3. 连接参数配置区域（config-grid）字段太多且一次性全显示，改为"正在使用的配置"只读展示 + 底部"编辑配置"按钮展开编辑区域

【协议配置页优化】
1. 规则列表（rule-list）中当前选中规则高亮不够明显，改为左侧竖线 accent + 背景色高亮
2. 协议表单（protocol-form）的示例帧（source-block）应该根据当前填写的包头 input 动态更新，而不是静态显示
3. 在规则列表下方加一个"测试包头匹配"区域：用户粘贴一段 HEX，系统自动高亮匹配到哪条规则（纯前端字符串匹配）

样式要求：整体保持现有深色主题（--bg, --panel 等 CSS 变量），新增元素使用已有色彩变量，不引入新颜色。